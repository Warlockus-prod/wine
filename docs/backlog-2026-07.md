# Vinovigator — backlog (обновлён 2026-07-06)

Сделано ранее — в `docs/audit-2026-07.md`. **Сделано 2026-07-06** (этот заход):
эмиттеры аналитики → /api/events (конец-в-конец, slug→id на сервере) · taste_profiles
UNIQUE + настоящий upsert · visual-снапшоты стабилизированы/перегенерированы ·
UI-аудит моб+десктоп и фиксы «разъезда» (невидимые серые тексты на креме, коллизия
вкладок VINOKOMPAS, клип лейблов колеса, navy-на-navy в БД-редакторе, 26 aria-label
в админке) · PL-вычитка: 23 худших фикса + сид обновляет reasons · ребрендинг
Bodega 1881 (Kraków) и Izakaya Senses (Warszawa) · парсер winnica.pl
(scripts/parse-winnica.mjs) → generated-каталог реальных вин с ценами/фото/отпечатками
Vinocompas + прямые ссылки и фото в предложениях самоучка. Ниже — что ОСТАЛОСЬ.

Легенда усилий: **S** <1ч · **M** полдня–день · **L** многодневная.

---

## ⏰ A. Срочное по времени / ops
- [ ] **TLS-сертификат истекает 2026-08-24** — попросить MetroWeb добавить `wine.icoffio.com`
      в edge auto-SSL (как остальные icoffio-домены). Иначе — ручное DNS-01 продление до дедлайна
      (процедура в CLAUDE.md). **Разовое, но с жёстким сроком.**
- [ ] **Админ-гейт выключен** (`AUTH_GATE_ADMIN=0`, намеренно) — включить перед коммерческим
      запуском: `AUTH_GATE_ADMIN=1` + `ADMIN_PASSWORD` в `.env.local` → редеплой. **S**

## 🧩 B. Требует твоего решения или данных
- [ ] **EN /samouczek** — решено: ДВА ЯЗЫКА → нужен полный EN-перевод туториала
      (контент wine-compass-kb + строки StagedTutorial/InteractiveCompass). **L, в работе**
- [x] ~~Curated-обоснования~~ — проверено: все 100 корректны; реальные разъезды были уровнем
      заведений → ребрендинг Bodega 1881 + Izakaya Senses (2026-07-06). 8 low — сомелье.
- [x] ~~PL seed-переводы (worst)~~ — 23 худших исправлено (finisz, taninowa, masło palone…);
      остаток ~40 medium в docs/data-review-2026-07.md → финальная вычитка носителем.
- [x] ~~Цены/фото вин + матчер 50+~~ — parse-winnica.mjs: реальные вина winnica.pl с ценами,
      фото и родными Vinocompas-отпечатками → winnica-catalog.generated.ts (2026-07-06).
      Осталось: те же реальные вина в КАРТЫ РЕСТОРАНОВ (сид) — вместе с «ценами до гостя» (C).

## 🛠 C. Инженерка (решения не требует)
- [ ] **Цены вин write-only до гостя** — сид пишет `price 0`, read-path не читает колонку, UI
      показывает захардкоженное → правки цены в БД-редакторе не доходят до гостя. Нужно: сид real
      prices + read-path читает price + UI использует. **M**
- [x] ~~Эмиттеры аналитики~~ — конец-в-конец работает (батч-очередь, sendBeacon,
      restaurant_view/dish_select, серверный slug→restaurant_id) — 2026-07-06.
- [x] ~~Поля без label в /admin~~ — 26 aria-label добавлено в сандбокс (2026-07-06).
- [ ] **GDPR-поверхность** — страница политики приватности + cookie/consent + retention (ЕС,
      сбор псевдонимных профилей). **M**
- [ ] **Распил админ-монолита** `admin/page.tsx` (1471 строк, всё в одном стейте — каждый
      keystroke ре-рендерит обе панели). **L**
- [x] ~~taste_profiles unique-constraint~~ — миграция 0002 + настоящий ON CONFLICT upsert (2026-07-06).
- [ ] **events: план retention/партиционирования** (append-only, растёт неограниченно). **M**
- [ ] **A11y-хвосты**: порядок заголовков (H1→H3), `<ul>` из `li[role=button]` (меню),
      scrollable-region не фокусируется с клавиатуры, контраст плейсхолдеров в админке. **M**
- [ ] **Тесты**: API-уровень для 13 роутов (валидация/ACL/rate-limit 429); e2e на
      InlineProposals + админ-редактор. **M** (visual-snapshot стабилизирован 2026-07-06 ✓)

## 🚀 D. Фичи (ранжировано по ценности для питча)
- [ ] **Дашборд владельца** `/admin/restaurants/[slug]/stats` — что гости выбирают (нужны
      эмиттеры из C). Главный аргумент продажи. **M**
- [ ] **Печать QR из админки + `?table=`** — питч обещает «wydrukuj, zalaminuj», печатать нечего. **S**
- [ ] **Импорт винной карты** (вставил текст/CSV → LLM → черновики строк) — крупнейший барьер
      онбординга (сейчас 60 вин = 60 форм). **M**
- [ ] **Профиль самоучка → /pairing** — `/api/profiles` готов и мёртв; связать localStorage-профиль
      с ранжированием у ресторана = персонализация. Сшивает воронку. **M**
- [ ] **Taste-along (дегустация за столом)** — методологический пробел; вся «Metoda degustacji»
      уже в KB прозой. **M**
- [ ] **UTM/ref на исходящих ссылках winnica.pl** — лицензиар метода не видит трафика от вас. **S**
- [ ] **«Pokaż kelnerowi»** — экран-финал после выбора вина (KPI ресторана = налитый бокал). **S**

## 🎨 E. Полировка (опционально)
- [ ] **Пиксель-в-пиксель под winnica** — точные cream/camel hex, отступы/радиусы карточек из
      Figma, если нужно 1:1 совпадение (сейчас визуально совпадает). **S–M + данные**
