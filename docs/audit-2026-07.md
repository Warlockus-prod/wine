# Full-project audit — 2026-07

10-agent read-only audit (backend, DB, frontend, security, perf, tests, a11y, UX, product, completeness).
Totals: **1 critical, 21 high, 37 medium, 25 low**. This doc tracks each actioned item and what remains.

## ✅ Fixed & deployed (2026-07-03)

### Critical
- **next/image optimizer re-enabled** — global `images.unoptimized` + 6 per-`<Image>` `unoptimized` props were shipping 1024px PNG masters. Guest restaurant page **21.9 MB → 0.11 MB** images (verified via `/_next/image`, avif/webp at 36–96px).

### High
- **apiHandler double-wrap** — write routes returned empty `{}`; now pass their real `NextResponse` through.
- **Rate-limit IP spoofing** — `clientIp()` now prefers non-spoofable `X-Real-IP` (nginx overwrites; 443 is TCP-passthrough), falls back to the LAST `X-Forwarded-For` entry.
- **Chat OpenAI cost-cap bypass** — keyed on the trustworthy IP, not the client-minted `anonymousId`.
- **Postgres backups** — `/opt/backups/wine_web_wn_backup.sh` + cron (03:30 daily, 14-day retention). Was ZERO backups. Verified 39K dump.
- **Guest QR landing** — pairing sheet no longer auto-expands over the menu (skip mount auto-select).
- **Duplicate OpenAI calls** — pairing explain effect keyed on top-wine id → 1 call not 3 per dish.
- **Unsplash thumbnails** — top-3 wine cards use the id-keyed local bottle map, not stock.
- **MobileTabBar** — localized (Odkrywaj/Łączenie); Admin tab hidden from guests; locale-correct pathname; both nav landmarks labeled.
- **Compass keyboard** — level-2 sektor + level-1 base wedges get `onKeyDown` (were click-only, WCAG 2.1.1).
- **Real 404** — restaurant page calls `notFound()` on unknown slug (was soft-404 / HTTP 200).
- **Error boundaries** — `[locale]/error.tsx` + `global-error.tsx` (branded bilingual apology; was Next's raw error screen).
- **Pitch copy de-staled** — Mapbox (not Leaflet+OSM), live DB edits (not "no backend / localStorage-only").

### Medium / Low
- OpenAI clients: `timeout: 20s`, `maxRetries: 1`.
- Curated pairing score: `+12` bonus preserving spread (was floored to 88 → guest saw 88/88/88).
- Deleted ~500 lines dead code (`restaurant-store.ts`, `CompassExplorer.tsx`) + dead Material Symbols CSS.
- FloatingTasteChat dialog: focus moves in on open, Escape closes, `aria-modal`.
- Chat textarea: `focus-visible` ring (was `outline:none`).
- Restaurant bottom-sheet drag handle: accessible name.
- `usePairingDataset`: seed-then-hydrate → fixed React #418 hydration mismatch on /admin.
- `GET /api/profiles`: 60/min IP rate limit + UUID validation (was unauth/unlimited/unvalidated).
- Home directory empty state localized.

## ⛔ Remaining — needs a decision
1. **EN `/samouczek` serves Polish** (high) — method is PL-native. Full EN translation (L) vs make PL-only + drop from EN sitemap (S).
2. **Curated pairing reasons describe the wrong dishes** (high) — seed-data quality; needs a sommelier/human pass.
3. **Which new features to build** (see below), in what order.

## 🔜 Remaining — larger lift (no decision needed)
- **Wine prices write-only** (high) — seed writes `price 0`; guest read-path drops the column & hard-codes display prices → DB edits never reach guests. Needs seed real prices + read-path reads price + UI uses it.
- **Analytics emitters** (high) — `/api/events` + table ready, no client emitters wired; also the id story (guest ids are `external_id`, route wants UUID) → send via `props`.
- **62 unlabeled admin inputs / 20 unnamed selects** (high, axe critical).
- **GDPR surface** (high) — privacy policy + consent + retention for an EU product storing pseudonymous profiles.
- **aria-live on pairing re-rank/proposals**; menu `<ul>` of `li[role=button]` semantics; scrollable-region focusability; heading order.
- **Perf**: gate mapbox-gl (1.7 MB) behind interaction; `Cache-Control` on `/dishes|/wines|/senses`; shrink 163 MB `public/` PNG masters; bundle-size guard in `npm run check`.
- **Tests**: zero unit tests (pairing scorer, cosine matcher, dryness) + zero API tests (13 routes); mask dynamic regions in `mobile-visual` snapshot; admin-editor + chat e2e.
- **`taste_profiles` unique constraint** (empty table now — do it with the profile→pairing feature); events retention/partitioning; sitemap from DB read-path (not seed); `published` bypass via direct slug.
- **Split the 1471-line `/admin` monolith**.
- **TLS cert expires 2026-08-24** — ask MetroWeb to add wine to edge auto-SSL (see CLAUDE.md renewal ritual).

## 💡 Proposed features (ranked value/effort for the restaurant pitch)
1. Owner analytics dashboard `/admin/restaurants/[slug]/stats` (M) — needs emitters first.
2. Printable QR sheet in admin + `?table=` param (S).
3. Wine-list import (paste/CSV → LLM → draft rows) (M) — biggest onboarding barrier.
4. Guest compass profile → `/pairing` personalization (M) — `/api/profiles` exists but is dead.
5. Taste-along (degustation) mode at the table (M).
6. UTM/ref on winnica.pl outbound links (S).
7. Expand the 19-wine matcher table from the winnica.pl catalog (M).
8. "Pokaż kelnerowi" order-handoff screen (S).
